import 'package:flutter/material.dart';
/*
class LibDialog extends StatefulWidget {
  const LibDialog({Key? key}) : super(key: key);

  @override
  State<LibDialog> createState() => _LibDialogState();
}

class _LibDialogState extends State<LibDialog> {
  @override
  Widget build(BuildContext context) {
    return Dialog(
      elevation: 0,
      backgroundColor: Color(0xffffffff),
      shape: RoundedRectangleBorder(
        borderRadius: BorderRadius.circular(15.0),
      ),
      child:
      Column(
        mainAxisSize: MainAxisSize.min,
        children: [
          const Text(
            "",
            style: TextStyle(
              fontSize: 18.0,
              fontWeight: FontWeight.bold,
            ),
          ),
          const Text(""),
          Divider(
            height: 1,
          ),
          SizedBox(
            width: MediaQuery
                .of(context)
                .size
                .width,
            height: 50,
            child: InkWell(
              highlightColor: Colors.grey[200],
              onTap: () {},
              child: Center(
                child: Text(
                  "Continue",
                  style: TextStyle(
                    fontSize: 18.0,
                    color: Theme
                        .of(context)
                        .primaryColor,
                    fontWeight: FontWeight.bold,
                  ),
                ),
              ),
            ),
          ),


        ],
      ),
    );
  }
}
*/